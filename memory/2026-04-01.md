# 2026-04-01

- Feishu 使用结构已明确分为两层：
  - 私聊 = 主助理 / 总控台：负责长期规则、偏好、跨项目协调、结构调整、提醒和总结。
  - 各个 Feishu 群 = Project bot / 专项工作台：每个群承接一个项目或主题的具体上下文，减少上下文混杂。
- 使用规则：
  - 不知道放哪，先发私聊。
  - 一个会长期持续的话题，单独建群。
  - 私聊管“怎么组织”和“长期规则”，群里管“具体怎么做”和项目执行。
- Feishu 群响应机制：
  - 新群如果我不响应，通常是因为该群的 group chat_id 还没加入 `channels.feishu.groupAllowFrom`。
  - 处理方式：用户在私聊中说“新建了一个群，你查日志把群 ID 加进去”，然后从 `/tmp/openclaw/openclaw-YYYY-MM-DD.log` 查最新 group chat_id 并补到 `/home/ubuntu/.openclaw/openclaw.json` 的 `channels.feishu.groupAllowFrom`。
- 当前已加入 allowlist 的群 ID：
  - oc_c9b64b365bd6111ae3601ac52ec21b63
  - oc_5329164ce0ae64a0f551e53dfddae096
  - oc_97afcd7d9686013d775c200e5a134c75
  - oc_fc747d291af6b88d7fa168f16ac88f6a
- 当前策略：
  - `channels.feishu.groupPolicy = "allowlist"`
  - `groups."*".requireMention = false`
  - 也就是：允许列表内的群中，老板发消息时不需要 @ 也能触发。
- 可直接复制的超短备忘录：
  - 私聊 = 主助理 / 总控台，管长期规则、偏好、跨项目协调。
  - 群 = Project bot / 专项工作台，每个群负责一个项目或主题。
  - 不知道放哪，先发私聊；会长期聊的事，就单独建群。
  - 私聊管“怎么组织”，群里管“具体怎么做”。
  - 新群没反应 = 还没把群 ID 加进配置，来私聊叫我查日志补上。
- 未来 agent / 工具选型备忘（当前不执行，条件成熟后再落地）：
  - 扩展 agent 时，优先补短板，不追求堆数量。
  - 当前主力体系是 Codex + OpenClaw 主助理。
  - 未来最优先考虑补强：Claude Code（大仓库理解、重构规划、多文件大改动）。
  - 次优先考虑：Gemini CLI / Gemini Code（超长上下文、大文档整理、资料归纳）。
  - 不建议一口气装太多同质化 code agent，2 到 3 个主力足够。
  - 默认分工倾向：Codex 负责执行落地，Claude Code 负责架构/重构分析，Gemini 负责资料归纳。
- 未来 AI SaaS 模板选型框架：
  - 先判断项目目标更偏“快上线”还是“长期可控”。
  - 如果重点是快速启动、尽快做出 MVP、优先产品壳子与上线速度：优先看 ShipAny / ShipFast。
  - 如果重点是开源可控、长期演进、适合 agent 深度重构：优先看 OpenSaaS。
  - 可以用一句口诀记：要快，先看 ShipAny / ShipFast；要控，先看 OpenSaaS。
  - 以后真要选型时，再按具体项目目标决定，不提前重投入研究。
- In Feishu group chat with allowlisted sender, discussed building a compliant forum/community promotion assistance workflow for the user's web projects. Boundaries emphasized: no spam, no fake identities, no bypassing platform verification or risk controls; assistant should only help with normal promotional actions.
- Diagnosed initial blocker for browser automation: OpenClaw/browser tool existed but no supported local browser was installed. Installed Chromium on Ubuntu 24.04 (snap-backed chromium-browser path `/usr/bin/chromium-browser`) and verified OpenClaw browser detection.
- Repaired an OpenClaw operator/control-plane pairing issue that caused approval failures (`pairing required` / expired approval flow). After fixing pairing, approval submissions still proved flaky, so local exec allowlist was used to continue necessary package installations.
- Determined OpenClaw browser startup was failing on the headless server because the machine had no DISPLAY and browser defaults were not suitable. Adjusted browser configuration to headless with explicit Chromium executable path, restarted gateway, and verified browser control could open pages.
- Evaluated manual-login options on a pure console server. Decided against full desktop environment and converged on a lighter “3B” approach: single-browser visual access instead of full remote desktop.
- Implemented Phase 1 of the 3B approach: installed `xvfb`, `x11vnc`, `novnc`, and `websockify`; created a virtual display (`:99`), launched Chromium in that display with a dedicated profile at `/home/ubuntu/browser-visual-profile`, exposed VNC on `5901`, and exposed noVNC on `6080`.
- Because the user was not on the same network as the server, direct noVNC access was not possible. Chose temporary-access option C and used SSH tunneling as the recommended method. User confirmed SSH tunnel to `127.0.0.1:6080/vnc.html` worked and they could see the remote browser.
- User successfully logged into a target site inside the visual Chromium instance. Verified that login-related files (`Default/Cookies`, `Default/History`, `Default/Preferences`) were written under `/home/ubuntu/browser-visual-profile`, indicating session state persisted on server disk.
- Important current technical state: the visual Chromium instance is separate from OpenClaw’s default controlled browser instance. OpenClaw still sees its own older CDP/browser session, while the visual instance currently lacks a remote debugging/CDP port, so it cannot yet be directly attached/controlled by OpenClaw.
- Agreed next milestone is Phase 3: convert the visual Chromium into a “visual + attachable” instance by restarting it with a fixed `--remote-debugging-port`, then configuring OpenClaw `browser.profiles` (named profile such as `visual`) to attach to that instance while reusing the same user-data-dir/profile.
- User preference learned during the session: whenever an approval command is needed, send it alone in its own separate code block for easy copying; also proactively report background-task completion/failure/blockers without waiting to be asked.
